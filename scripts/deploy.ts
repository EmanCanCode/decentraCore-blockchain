// will deploy and seed all deployed contracts
// have a delimiter for each contract to know if we are deploying or not
// e.g ({ realEstate: true/false, finance: true/false, supplyChain: true/false, ... })
